import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create an oath",
  description: "Define campaign demands, deadlines, evidence thresholds, and GEN allocations for a new HORKIOS oath.",
};

export default function CreateLayout({ children }: { children: React.ReactNode }) { return children; }
