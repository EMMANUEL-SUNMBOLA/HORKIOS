import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "How HORKIOS works",
  description: "See how HORKIOS turns public campaign terms and evidence into programmable, verifiable settlement.",
};

export default function HowItWorksLayout({ children }: { children: React.ReactNode }) { return children; }
